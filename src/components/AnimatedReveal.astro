---
type Props = {
  delay?: string;
  class?: string;
};

const { delay = '0', class: className = '' } = Astro.props;
---

<div class:list={[className]} data-reveal data-reveal-delay={delay}>
  <slot />
</div>
